extract generic utilities out to non-version specific utils module
    i think the general raw -> voltage and index works between versions for the most part
    stuff like that
extract version specific stuff into version specific module
    for example, version 4's sample rate is implied Hz
create a unified return value schema
    for example, the different versions can use different units:
        my scope's volt/div is measured in V, but in their examples, it's mV/div, so force it into V/div
        my scope reports its sample rate in Samples/s, but some models may report in MSamples/s
        just normalize everything to unit i guess

